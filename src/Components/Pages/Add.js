import React from 'react'
import Page from '../Page'

// import PropTypes from 'prop-types'
// const propTypes = { }

function Add(props) {
  return (
      <Page render={() => {

        return (
          <h1>Add</h1>
        )
      }} />
  )
}

// Add.propTypes = propTypes

export default Add